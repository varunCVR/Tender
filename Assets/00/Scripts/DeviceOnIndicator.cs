using System;
using System.Net;
using System.Text;
using System.Xml.Linq;
using UnityEngine;
using UnityEngine.UI;
/*using uPLibrary.Networking.M2Mqtt;
using uPLibrary.Networking.M2Mqtt.Messages;*/

public class DeviceOnIndicator : MonoBehaviour
{
    /*[Obsolete]
    MqttClient client = new MqttClient(IPAddress.Parse(@"192.168.0.2"));
    System.Random rnd = new System.Random();
    private string clientId;

    private string deviceIdString = " ";
    public static string deviceId;

    // Start is called before the first frame update
    void Start()
    {
        deviceIdString = "dY0536";
        deviceId = deviceIdString;
        clientId = deviceId;
        UnityEnDebuggine..Log(clientId);
        client.Connect(clientId);

        client.Publish("connected/nilVR", Encoding.UTF8.GetBytes(deviceId + " on"), MqttMsgBase.QOS_LEVEL_EXACTLY_ONCE, false);
    }*/
}
